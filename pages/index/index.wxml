<!--index.wxml-->
<view class="container">


    <view id="bbbb" class="banner" animation='{{slideAnimData}}' >
      <view class="item" style="left:{{left}}rpx" catchtouchstart="touchstart" catchtouchmove="touchmove" catchtouchend="touchend" touchcancel="touchcancel"></view>
      <view class="delete">删除</view>
    </view>





    <view bindtap="bindViewTap" class="userinfo" abc="aaa">
        <image class="userinfo-avatar" src="{{userInfo.avatarUrl}}" background-size="cover" animation="{{animationData}}" catchtap="test"></image>
        <text class="userinfo-nickname">{{userInfo.nickName}}</text>
    </view>
    <view class="usermotto">
        <text class="user-motto">{{motto}}</text>
        <text class="user-motto">{{xinjun}}</text>
    </view>

    <view wx:for="{{list}}" wx:key="*this">
        {{index}}: {{item}}
    </view>



    <view class="bottom_bar">

        <view class="tab" bindtap="tabMainClick">
            <image src="{{tab_main_src}}"></image>
            <text style="color:{{text_color_home}}">主页</text>

        </view>
        <view class="tab" bindtap="tabMeClick">
            <image src="{{tab_me_src}}"></image>
            <text style="color:{{text_color_me}}">我的</text>
        </view>



    </view>



</view>